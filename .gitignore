# 环境
.cursor
.venv
.vscode
.agent
.idea
.claude
.env

# 构建文件
build
dist
dist_release
*.pyc
*.pyd

# 运行时生成的文件
*.dll
!3rdparty/PaddleOCR-json_v1.4.1/*.dll
build.bat
config.yaml*
theme_pack_list.yaml
logs
pythonlogs
i18n/*.qm
__main_dev_temp__.py
/*.png
theme_pack_weight*
theme_pack_weight/*

# 测试文件
/test/
/test*.py

# 本地 issue 追踪
/issues/

# 更新清理测试垃圾
/assets/garbage_test/

# 解包的游戏资源（由外部工具生成，不纳入版本控制）
/assets/extracted/
# 存档的游戏资源（由外部工具生成，不纳入版本控制）
/archive/

# 其他
MaaFramework
.worktrees

# Asset library — recycle bin, scan cache & thumbnail cache (large binaries, local-only)
data/asset_library/recycle/
data/asset_library/scan_cache.json
data/asset_library/thumbnails/
assets/app/fonts/chars.txt
